pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url = uri("https://jitpack.io") }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url = uri("https://jitpack.io") }
    }
}
rootProject.name = "Awesa"
include ':app'
include ':awesalibs'
//include ':ffmpeg-kit-android-lib'

gradle.ext {
    compileSdkVersion = 35
    targetSdkVersion = 35
    minSdkVersion = 28
}

